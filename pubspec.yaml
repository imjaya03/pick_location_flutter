name: pick_location
description: Package that provides Place search and Location picker for flutter app.
version: 0.0.2
homepage: https://github.com/imjaya03/pick_location_flutter

environment:
  sdk: ">=2.19.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
